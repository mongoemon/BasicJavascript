//const keep same value, does not update value itself
//const can have only one and apply entire sheet
//not suitable to update data

const flag = true
//but we can call reserve value
if(!flag)
{
	console.log("Condition is true")
}
else
{
	console.log("Condition is false")
	//compile goes here, const does not update the new value, just call reverse value
}
